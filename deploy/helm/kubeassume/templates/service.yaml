{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeassume.fullname" . }}-metrics
  labels:
    {{- include "kubeassume.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.metrics.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "kubeassume.selectorLabels" . | nindent 4 }}
{{- end }}
